#!/usr/bin/python3
def max_integer(my_list=[]):
    if my_list:
        amax = my_list[0]
        for a in my_list:
            if a > imax:
                amax = a
        return amax
    return None
